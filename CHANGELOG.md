# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.10.1] - 2026-09-19

### Added（开发工具进包）

- **`scripts/unzstd-frames.mjs`**：多帧 zstd 解压——读 DSH 会话日志的必经一步。DSH 的 `session.v3.jsonl.zstd` 是**多帧拼接**的结果，而 `zlib.zstdDecompressSync()` 只解**第一帧**：实测 2.2 MB 的日志有 **639 帧**，整段解只拿得到几百字节，看起来像「日志是空的」，极易误判。脚本按帧 magic 切分后逐帧解压，头部写明用途 / 判据（`frames` / `ok` / `failed` 三个数）/ 局限（magic 扫描会假阳性），配 `--help` 与语义化退出码（0 = 全解出 / 1 = 有帧失败 / 2 = 用法错误），并写进 `files`。
- README「开发」段补上两个脚本的用法（`scan-orphans` 与 `unzstd-frames`）。

### Fixed

- 修掉归档来源脚本里的一条容错分支：`zlib.createZstdDecompress()` 是**异步**的，在同步块里 `end()` 之后立刻取 chunks 必然拿不到数据——宁可如实报告失败，也不返回一个看似成功的空结果。

## [0.10.0] - 2026-09-19

### Fixed（提示词的自足性：陌生环境装上去也能用）

以「一台陌生机器第一次装上本插件」的视角复查提示词，查出两处会导致**沉默失效**的缺口：

- **namespace 判据完全缺失**：`memory_save` 的 namespace 参数说明只有一句 `Where it applies; defaults to global.`，GUIDANCE 与自述里都没有「该记哪一层」的判据。后果：陌生环境的模型会全写 `global`，`project` 层永远空着——而「记忆跟随仓库分享」正是 project 层的存在理由。现在 GUIDANCE 补上判据问句：**「换个项目，这条还成立吗？」** 成立 → `global`（偏好 / 习惯 / 环境知识）；不成立 → `project`（本仓库自己的约定、架构决策、它踩过的坑）。
- **自述的工具清单少了 4 个**：写着「你有 6 个记忆工具」，实际注册 **10** 个，漏掉 `prompt_search` / `prompt_get` / `prompt_list` / `prompt_add`。改为 10 个并说明模板通道（md 文件、**永不注入**、不会混进记忆检索）；同时补两句：`memory_confirm` 的使用边界（仅在用户明确要求放行隔离记录时用）、两层记忆的定位（`project` 落在 `<cwd>/.dsh/storages/`，随 git 提交分享）。
- **根因记在设计文档 §2.6**：本机上的「好用」是提示词与数月积累的记忆共同作用的结果，提示词单独拿出来不自足。这直接是跨工作区可见性方案（④/①）的前提——那两块都以 project 层有内容为基础。
- 机制层的前提已逐项验证：目录自动创建、空库不产生空注入块、自述与指引是常量、工具 schema 全量注册。
- GUIDANCE 增至 1711 字符。测试无改动（既有用例只断言 section / context 存在与含 `dsh-memory v`、`memory_update`，不锁文案）；typecheck 干净，103 例全绿。

### Added（跨工作区可见性与分级索引：④-A / ④-B / ①）

补的是同一个盲区——**「不知道存在」**：检索是有意图的动作，搜不出自己不知道存在的东西。三个方向分别对应「父目录有什么」「子目录有什么」「库里一共有什么」。

- **④-A 祖先链（检索侧 + 定位侧）**：project 记忆按会话 cwd 分文件存放，而读写路径原先只认当前 cwd 那一张表——父目录会话读不到子目录的记忆，反过来也一样。现在 `list` / `search` 把候选扩为「工作区链」（cwd 自身 + **文件已存在**的祖先层级），祖先记录带 `..` / `../..` 来源标记（工具输出与注入行同格式）。**路径一律经 `join()` 规范化**——`projectBackendName` 对字符串敏感，`H:\a\b` 与 `H:/a/b` 会算出不同文件名。三条边界：祖先只收已存在的层级（否则会凭空创建空记忆文件）、写入仍只落当前 cwd、定位类操作（`update` / `forget` / `setStatus` / `setInjected`）也走链（**检索得到就要改得动**）。命中记账同样跨链——否则祖先记录被跨区命中却不记账，会被 30 天规则误撤。**注入路径保持不变**（只读当前 cwd），这是设计决定而非遗漏。
- **④-B 子项目索引行**：注入里多一行 `（本工作区下另有 N 个子项目带记忆：xxx（M 条）——用 memory_search 检索）`。扫描在异步预热路径完成、结果缓存（注入 provider 是同步回调，只能读缓存），并发调用与预热合流而非重复扫描；`reload()` 时随存储一并作废。**不占注入预算、自消除**（没有子项目记忆时整行不出现）。只扫一层、只报条数——不递归、不搬运内容。
- **① 记忆索引行**：注入里多一行 `（索引：当前可见 N 条记忆，主题集中在 X(14)、Y(12)…——用 memory_search 检索）`。主题取自各条的 `keywords` 词频（**与检索用的是同一套词汇**，所以索引里出现的词就是能搜到的词），同频按字典序排以保证输出确定。**不做成一条常驻记忆**（那会自己占预算、参与零和竞争、且记忆增删后不会自动更新），而是系统生成的行：不占预算、不可能腐化。**长度常数级**——主题数固定上限，条数只报数字（用例断言 10 条 → 1000 条时行长增长 ≤ 8 字符）。
- 新增 21 例测试（`workspace-chain.spec.ts` 8 + `neighbor-notice.spec.ts` 7 + `index-notice.spec.ts` 6）。**负向验证逐项实测**：关掉祖先收集 → 5 红 106 绿；发现永远为空 → 3 红 115 绿；索引行永远为空 → 4 红 120 绿；恢复后各回全绿。
- 设计文档：`docs/设计/2026-09-19-跨工作区可见性与分级索引-设计方案.md`。

### Added（④-C 孤儿记忆清点）

- **新增只读清点脚本 `scripts/scan-orphans.mjs`**：报出「哪些记忆文件的命名当前代码已经不认识」。背景——文件名为 `memory_project_<djb2(工作区路径)>.json`，历史上换过命名（早期无哈希后缀、有过双重 `memory_project_` 前缀），旧文件因此**永远打不开**：不是「等人放行」，是代码不认识这个文件名了。
- 实测（2026-09-19）：全工作区 6 个 storages 目录里，**3 个文件能被代码打开、4 个是孤儿共 34 条记录**（`WorkStation` 的双重前缀旧名 1 条、`deepseek-harness` 的无后缀旧名 16 条、`dsh-web-runtime` 的同两份拷贝 17 条）。
- **脚本不做任何处置**：不迁移、不删除、不修改。旧文件可能含 0.7.0 之前未过 `detectSensitive` 的内容，自动导入等于绕过隔离检查——处置是一次显式的人工动作，见设计文档 §3.4。

## [0.9.2] - 2026-09-19

### Fixed（两处常驻提示词与实际机制脱节）

- **`GUIDANCE` 里「常驻注入是人工开关」是错的**（`tool:memory` section，每轮进 system prompt）。原文写 *whether it is injected every turn is a separate human-controlled switch (`injected`)*——0.8.0 起模型侧可经 `memory_save` / `memory_update` 的 `injected` 参数显式钉住；旧表述会让模型以为自己没有该权限，也与同段「人工只在例外时介入」自相矛盾。改为写明该参数可用、写的是与设置面板同一个开关，并给出判据（规则 / 长期约定 / 判据常驻；事实 / 参考 / 案例留检索）。
- **同段「审核通过只代表内容被认可」已失去对象**：0.7.0 静默记忆机制起没有逐条人工放行流程，删除。
- **自述（`memory:self`）停在 0.5.2 的框架**：只写「受预算限制」一句——模型既不知道开关自己能开，也不知道预算有多紧。补齐四件：两条自动规则的阈值（命中 2 次开 / 30 天未命中撤）、显式接管语义、预算量级（默认 1500 字符 ≈ 11 条、装不下的**整条丢弃**），以及预算诊断行的含义——它是**「有常驻记忆并不在场」的唯一信号**，看到就该逐条评估撤下。
- 成本：两段都是 system prompt 固定前缀，`GUIDANCE` 增约 350 字符、自述增约 630 字符；固定前缀命中提示缓存，代价可忽略。判据在两处各写一遍是有意的——它们是改变默认行为的行为约束，重复强化优于单点缺失。
- 无测试改动：既有用例只断言 section / context 存在、自述含 `dsh-memory v` 与 `memory_update`，不锁文案。typecheck 干净，103 例全绿。

## [0.9.1] - 2026-09-18

### Changed（诊断行报短摘要，不再只报 id）

- **预算诊断行从「列 id」改为「列短摘要」**（每条约 24 字符）。起因是一次自查：诊断行的用途是**扫一眼发现「出局者里有没有跟当前任务相关的」**，而 id 得再查一次才知道是什么——「主动去查」恰好是最容易省略的那一步，省略了就等于白报。实测中拿到 `7bcaf99d, 3a8999c1, …` 这串 id 后，确实是回头读存储文件才认出它们是哪八条的。
- 内容量：8 条约 160 字符（原来 8 个 id 约 100 字符），**仍不占注入预算**。
- 常量改名 `OMITTED_ID_LIMIT` → `OMITTED_NOTICE_LIMIT`（它限制的是列出条数，不再限于 id）；新增 `OMITTED_SUMMARY_CHARS = 24`。
- `injectionPreview` / 面板的 `omittedRecords` 多一个 `summary` 字段（`id` / `line` 保留，无破坏式变更）。
- 测试更新 3 例。**负向验证已实测**：把摘要换回 id，3 例红在各自预期位置——端到端 `to contain '较早的常驻约定'`、纯函数 `to contain '适配纪律'`、截断例 `match` 返回 null；其余 10 例照旧为绿。
- 端到端用例改用**行首正则** `^- \[memory:` 区分「进了注入行」与「只出现在诊断行」——0.9.1 起出局者的摘要也会出现在文本里，只断言内容包含会混淆两者。

## [0.9.0] - 2026-09-18

### Added（预算诊断：哪些常驻记忆实际没进上下文）

- **注入末尾新增预算诊断行**：常驻装不下时，`memory:recall` 追加一行 `（另有 N 条常驻因预算未注入：id1, id2, …）`。此前这个差额**无人可见**——模型不知道（注入上下文里既没有它们、也没有说明），用户不知道（面板只显示「省略 N 条」这个数字，没有明细）。「钉住常驻」被默认理解成「每轮在场」，而在 1500 字符预算下它只是「有资格排队」：实测 19 条 `injected: true` 只有 11 条真进场。
- **诊断自消除**：只在 `omitted > 0` 时出现，清理干净即消失；**不占注入预算**（否则会递归：诊断挤掉记忆 → 出局更多 → 诊断更长）；id 截断到前 12 个，防这行本身失控。
- **放在 `memory:recall` 而非 `memory:self`**：后者是每轮一字不差的固定前缀，塞动态内容会破坏提示缓存。
- **面板可展开明细**：`injectionPreview` 新增 `omittedRecords`（id + 渲染行，与注入行同格式），面板把省略计数做成可展开列表。原 `omitted` 计数保留不动，既有 client 不受影响。
- 新增 5 例测试（`test/injection.spec.ts` 3 例 + `test/injection-assembly.spec.ts` 2 例）：明细不进注入行、无省略返回空串、超限截断、端到端诊断行出现在组装结果里、预算充裕时不出行。**负向验证已实测**：把诊断行禁掉，端到端那例红在 `expected … to contain '另有 1 条常驻因预算未注入'`，其余 12 例照旧为绿。
- 端到端用例用 `vi.setSystemTime` 拉开两条记忆的创建时间，而不是 sleep——注入按「最近更新优先」排序，而 Windows 的时钟粒度可能让一次短 sleep 之后拿到的仍是同一个值。
- 设计文档：`docs/设计/2026-09-18-常驻注入预算诊断-设计方案.md`。

## [0.8.0] - 2026-09-18

### Added（模型侧可显式指定常驻注入）

- **`memory_save` / `memory_update` 新增可选 `injected` 参数。** 常驻注入此前只有两条路：面板开关（人工）与「命中累计 2 次」的自动升级（写 `injectedAuto: true`）。两条都覆盖不到**低频但关键**的记忆——长期约定、判据、委托这类内容不会被反复检索，够不到阈值；勉强够到也会被 30 天淘汰撤下。而它们恰恰是「没人想起来搜也必须生效」的那一类，只能靠人代点开关。
- **语义与面板开关一致**：给值即写 `injectedAuto: false`，此后既不被自动降级也不被自动升级——那是**有意的判断**，不是「用得多所以升」的统计推断。**省略参数则完全维持原状**：不注入，且连 `injectedAuto` 一起保持不动，自动升级资格不受影响。
- **边界**：隔离记录与未审核（`suggested`）记录不接受注入。写入/更新路径**静默降级为不注入**（顺带设置不该中断整次写入），而 `setInjected` 对隔离记录仍是**抛错**（用户明确要开，该被告知）——两条路径的越界处理刻意不同。更新内容命中危险规则时连带撤下注入。
- 注入位变化现在单独发一条 `memory/changed` 事件（此前 `update` 只发 `status`），客户端与面板据此刷新「常驻」列。
- 新增 7 例测试（`test/memory.spec.ts`）：显式钉住与省略语义、隔离边界、未审核门槛、`update` 省略时连 `injectedAuto` 一起保持、两条自动规则在显式决定后双向失效、工具层参数落到存储。**负向验证已实测**：把 `rememberInner` 的注入初值退回常量 `false`，3 例红在 `expected false to be true`，其余 43 例照旧为绿——用例锁住的是新行为本身，不是恰好为绿。

## [0.7.3] - 2026-09-17

### Fixed（重启后首轮不再缺工作区记忆）

- **两个并发的工作区打开会互相撞掉。** `projectTableFor` 的打开是异步的，而调用方会并发：`session/created` 的预热是 fire-and-forget，注入路径、工具路径、面板路径各来一次。底层 unit 句柄只允许一个，后到者以 `unit '…' is already open; a unit has exactly one live handle` 失败，而调用方普遍把预热失败当作无碍吞掉——于是表现为「这一轮工作区记忆缺席」。现在进行中的那次打开会被复用（`projectOpenings`），`reload()` 也会先等它们落地再关闭。
- **组装 waterfall 里补一次预热**：注入 provider 是同步的（`SystemPrompt.context` 的 `text` 类型即 `string | ((context) => string)`），重启后首次组装时工作区表还没打开，工作区记忆会整整缺席一轮。`system-prompt/assemble` 是异步 waterfall、且文档写明「返回值权威」，在那里等预热完成后重渲染这一条 context。
  - 不拿「此刻表是否打开」当作「provider 渲染时是否打开」的判据：provider 里那次预热的 Promise 可能在 `next()` 的 await 期间就完成了，看到「已打开」而跳过，就会漏掉真正需要补的那一轮。所以总是重渲染一遍（纯函数、读内存表），文本没变则原样返回。
- 新增 `test/injection-assembly.spec.ts`（3 例）。**负向验证**：停掉补预热那一步，首个用例红在 `expected '' to contain '工作区常驻记忆'`——正是生产上首轮那条 context 为空的样子。用例用「引擎从未打开过这个 cwd、存储文件按真实形状放在位置上」构造「刚重启」，而不是另建引擎（存储层不允许同一 unit 有两个活句柄）。

## [0.7.2] - 2026-09-17

### Fixed（重载不再把工作区记忆从上下文里弄丢）

- **`reload()` 现在会重开它关掉的工作区表，而不是一关了之。** 0.7.1 把重载从「在面板上手动点一下」变成「每次发现磁盘被别人改过就自动发生」，于是过去罕见的副作用变成了常见现象：注入 provider 是**同步**回调、只读已打开的表，表被关掉就等同于「这个工作区没有常驻记忆」——工作区记忆会从上下文里静默消失，直到某次工具调用把表重新打开。重开的表同样从磁盘重读，「放弃内存缓存」的语义不变，变的只是「重读完别忘了装回去」。
- 新增第 6 例「重载后工作区常驻记忆仍在注入集合里」。**负向验证**：去掉重开那一步，该例红在 `expected [] to include '工作区常驻记忆'`——注入集合直接空掉，与生产实例上看到的「重启后注入只剩 global」形状一致。

### 生产实例验证（0.7.1，2026-09-17 晚）

在一台真实运行的 SSiD 上（不是测试的临时目录）走了一遍完整冲突：备份 `memory.json` → 用脚本直接改写它（删掉一条 + 新增一条，模拟另一个实例的写入，不经过被测引擎的内存态）→ 让引擎写一条新记忆 → 对账。**外部新增的活了下来、外部删除的没被本地内存态带回来、本地新写的落了盘、原有记录一条不少**（32 → 33 → 34 条，差集只有预期的那三条）。验证用的标记记录已用 `memory_forget` 清掉，未留残留。

### 已知边界

- **重启后的第一轮**仍可能缺工作区记忆：注入 provider 是同步的，工作区表得有人先打开它（`session/created` 的预热是异步的，与首轮组装存在赛跑；恢复出来的会话也不会再走一遍创建路径）。这一轮之后自行恢复，数据层面无影响。

## [0.7.1] - 2026-09-17

### Fixed（多实例写入协调）

- **两个 DSH 实例共用同一个 `DSH_HOME` 时，不再互相抹掉记忆。** DSH 的存储层写入是「读—改—写全量覆盖」，且官方两个后端都声明**不做跨进程协调**（`storage-json`：「writer per process and last-write-wins is correct」；`storage-sqlite`：「cross-process coordination is out of scope」）。于是另一个实例（web 与 SSiD 桌面壳同时运行）写入的记忆只落在磁盘上，被本实例的下一次整份写入冲掉——**静默发生，没有任何提示**。
- 写入路径统一经 `writeScope`：串行化 → 开盘前比对存储文件指纹（mtime + size）→ 变过就重载 → 执行读改写 → 记指纹。等价于「update 前先 select」。覆盖 `remember`、`update`、`setStatus`、`setInjected`、`forget`、命中记账（`markUsed`）与三项启动维护（`migrateLegacy`、`demoteStale`、`verifyAnchors`）。
- 复审时补掉同批改动里的一处静默降级：`markUsed` 原读同步的工作区表缓存，而 `writeScope` 的重载会清空它——被清空后工作区侧的命中记账直接跳过（不报错、不丢数据，但「经常被翻出来」这条证据少记一次，而自动升常驻与 30 天淘汰都靠它驱动）。改为与其余写入入口一致地异步取表。
- 新增 `test/multi-instance.spec.ts`（5 例）：外部新增不被覆盖、外部删除不复活、`update` 前先 `select`、外部改写不被本地整份写入带回来、重载后工作区命中记账不丢。**两次负向验证均已实测**：禁用刷新门时前 4 例全红；把工作区取表退回同步缓存时第 5 例红在 `expected +0 to be 1`——用例锁住的是各自声称的行为，不是恰好为绿。

### 已知边界

- 协调分两层：进程内（写入串行链）与进程间（磁盘指纹触发重载）。残余窗口只剩两个实例在**同一瞬间**写——此时后写者赢；同时写**不同**记忆已不再互相影响。
- 指纹为 mtime + size，同毫秒且同字节数的两次外部写入无法区分（概率极低，且下一次写入仍会重载）。

## [0.7.0] - 2026-09-15

### Changed（行为变更；旧数据兼容）

- **写入即生效**：`memory_save` 不再只写「待审核」——低危内容直接 `approved`。人从「逐条放行」降为例外干预（钉住 / 删除 / 回滚）。`memory_update` 同样保持生效，不再把记录退回待审核。
- **`memory_confirm` 语义变更**：从「必经审核闸门」变为「放行被隔离的记忆」（同时解除隔离）。

### Added

- **危险内容硬拦（隔离位）**：写入或更新时命中密钥/凭据规则（8 类：常见平台密钥前缀、私钥块、赋值式秘密、认证头、带口令连接串等）的记录被隔离——**不进注入、不进检索**，直到人工放行或删除。
- **淘汰机制（信号驱动的常驻升降）**：记录新增 `hitCount`；命中达 2 次即自动打开常驻注入（`injectedAuto`）；超过 30 天未再命中则自动撤下。人工动过的开关双向豁免——既不自动升也不自动降，人是唯一能表达「不要这条常驻」的角色。
- **命中记账只认前几名**：一次检索最多把 BM25 前 5 名记为「被使用」。不设上限时命中计数会退化成「和查询有任意字符重叠」：BM25 对常见 2-gram 给全库打分，本机实测一句查询命中 81 条，三次测试检索就把 89 条记忆全升成常驻、占满注入预算。检索返回值不受此上限影响。
- **模板不再参与记忆检索**：`memory_search` 默认只搜 `fact` 记录，模板走自己的 `prompt_search` 通道（显式传 `kind` 的调用方照旧生效）；`recallRecords` 另加兜底，存储里即使存在被标了注入的模板也不进上下文。此前模板混在候选池里，既占返回条数，又会因「经常被搜到」被记命中——而模板正是明确不该进上下文的一类。
- **有效性锚点**：记录可声明锚点（`env` / `tool-list` / `self-version`），绑定到可探测的环境值；会话启动时批量校验，值变了即标 `stale` 并撤常驻，且在 `memory_search` 结果里带失效标注——过时的记忆不再被当成仍然正确。
- **会话启动维护**：每次会话首次打开工作区时做一次（幂等、失败不阻断）——旧数据迁移、长期未命中降级、锚点校验。
- **注入自述改为行为指引**：每轮注入的机制说明从「机制是什么」改为「何时写入 ①②③ / 不要记什么 / 怎么写关键词」，让模型自行维护记忆——不依赖人工闸门，也不引入后台分析管道。

### 兼容

- 全部新字段可选，**旧记忆文件照常读写**；旧枚举 `auto` / `suggest` 在读时迁移。
- **一次性迁移**：旧 `suggested` 逐条过危险检测——安全升 `approved`，命中则隔离；有 `lastUsedAt` 而缺 `hitCount` 的回填 1；旧 `injected: true` 且无 `injectedAuto` 的一律不动。迁移前把存储文件备份为 `.bak-<日期>-migration`。

## [0.5.2] - 2026-08-25

### Added

- **注入预算治理**：`memory:recall` 每轮注入改为单行摘要（首行截断，`summaryChars` 可配）+ 预算上限（`injectionBudget`，默认 1500 字符，超预算按最近使用/更新优先省略）；面板「注入预览」显示预算使用与省略条数，与真实注入同源渲染。
- **中文检索升级**：tokenize 对 CJK 连续段产出单字 + 2-gram（中英混合精度提升）；`memory_search` 改为 content 与 keywords 分离的加权 BM25（`bm25FieldScores`，人工关键词命中权重更高）；新增 `rrfFuse`/`cosineSimilarity` 导出。
- **记忆生命周期**：记录新增 `lastUsedAt`（memory_search 命中时写回），注入排序按最近使用优先，面板显示上次使用日期与冷数据提示（30 天未检索）。
- **混合语义检索**：可选 `embeddings` 配置（`embed` + 可选 `similarity`），启用后 memory_search 以 BM25 与语义结果做 RRF 融合，向量持久化到存储文件（`vector` 字段，明文可读）；缺省/失败自动降级纯 BM25。
- **工程债**：project 存储文件名规范化（去除双重前缀）——`memory_project_<hash>.json`，旧名文件打开时自动迁移（失败回退旧名，不丢数据）。

## [0.5.1] - 2026-08-25

### Added

- 工作区（project）记忆支持常驻注入：`memory:recall` provider 经 `AssembleContext.agent` 拿到当前会话 `header.cwd`，每轮注入 = global 的 approved+injected + **当前会话工作区**的 approved+injected，多工作区会话互不泄露。预热路径（`session/created` + provider 兜底）保证首轮或次轮即可注入；面板开关与注入预览（`/memory/api/injectionPreview`、`remote.memory.injectionPreview`）按当前会话工作区路由并标注 namespace。

## [0.2.0] - 2026-08-16

### Added

- 新增 `cordis.patch.yml`（bundle patch）并在 `package.json` 声明 `dsh.bundle`，支持通过 `dsh plugin add` 一键安装。

## 0.1.x（早期版本）

- 跨会话明文记忆：BM25 关键词检索（无向量嵌入）。
- `memory_save/list/search/forget` 工具与 `memory_confirm` 人工确认闸门（`suggested` → `auto`，模型永不自我提升）。
- 按 namespace 拆分 `global`（`$DSH_HOME/storages`）与 `project`（项目 `.dsh/storages`，随 git 分享）两层存储。
